class Ball{
    constructor(x,y,width,height,options){
     var options{
        'restitution':0.8;
         'friction':1.0;
         'density':1.0;
        }
        polygon = Bodies.circle(50,200,50);
        World.add(world,polygon)
    }
    rectMode(CENTER)
}